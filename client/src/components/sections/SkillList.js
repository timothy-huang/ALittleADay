import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.section`
  display: flex;
  flex-direction: column;
  margin-top: 16px;
  padding: 16px;
  width: 319px;
  background: #ffffff;
`;

const SkillsList = () => {
  return <Wrapper>This is the skill list section</Wrapper>;
};

export default SkillsList;
